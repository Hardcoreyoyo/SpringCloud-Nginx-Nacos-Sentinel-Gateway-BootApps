spring:
  application:
    name: SpringCloud-Gateway
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        port: ${DISCOVERY_PORT}
      config:
        server-addr: nacos:8848
        file-extension: yaml
        group: HARDCOREYOYO_GROUP